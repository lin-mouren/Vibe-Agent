name: E2E Smoke

on:
  workflow_dispatch:
  schedule:
    - cron: "15 */6 * * *"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: frame2
          POSTGRES_PASSWORD: frame2
          POSTGRES_DB: frame2
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U frame2 -d frame2"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://frame2:frame2@localhost:5432/frame2?schema=public
      REDIS_URL: redis://localhost:6379
      STORAGE_DRIVER: local
      LOCAL_STORAGE_DIR: ./.data/storage
      API_HOST: 127.0.0.1
      API_PORT: 4000
      NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:4000
      NEXT_PUBLIC_DEFAULT_CANVAS_ID: 00000000-0000-0000-0000-000000000001
      IMAGE_PROVIDER: mock
      IMAGE_PROVIDER_FALLBACK_TO_MOCK: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Enable corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm --filter @frame2/api prisma:generate

      - name: Prepare DB
        run: |
          pnpm db:push
          pnpm db:seed

      - name: Build runtime
        run: pnpm build

      - name: Start API and Worker
        run: |
          mkdir -p .workbench
          node apps/api/dist/src/index.js > .workbench/api.log 2>&1 &
          node apps/worker/dist/index.js > .workbench/worker.log 2>&1 &

      - name: Wait for API
        run: |
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4000/healthz >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          echo "API failed to become ready"
          exit 1

      - name: Run smoke suites
        run: |
          pnpm test:smoke:m3
          pnpm test:smoke:m4
          pnpm test:smoke:m5

      - name: Dump runtime logs on failure
        if: failure()
        run: |
          echo "===== API LOG ====="
          cat .workbench/api.log || true
          echo "===== WORKER LOG ====="
          cat .workbench/worker.log || true
